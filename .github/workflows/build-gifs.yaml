name: Build Gifs from Python

on:
  push:
    branches:
      - 'main'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: 
          actions/checkout@v4
      -
        name: Install dependencies
        run: bash .github/scripts/install-dependencies.sh